import { NgModule } from '@angular/core';

@NgModule({
  // We've emptied this module since we're moving the routing elsewhere
  // The standalone components will be loaded directly through the route configuration
})
export class MunicipalityModule {}
